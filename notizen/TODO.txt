Sound Processes

- [NO] (( mapan : line synth sollte wohl nicht freeSelf machen, sondern trigger senden, so daß client den richtigen endwert setzt, before audio-bus leer wird? // irgendwie scheint es klick-los zu gehen...? (warum?)
---> der letzte wert wird definitiv gehalten, also kein problem ))
- numChannels changes
- scalar controls (pString)
- bufEmpty


Nuages

- bypass filter
- insert filter (a: new; b: existing via drag)
- meter
- gui update glide values
- tablet support
- disconnect mappings
- dispose proc
- selectable proc (evtl. aggregate?)
- lazy slider for xfade/ glide
- how to implement NuagesULoop?
// vielleicht radikal anderes konzept mit
// einem globalen buffer-pool?
- soloing
- control-room recorder
- diskin based procs: track playback position
- smc: freesound proc
